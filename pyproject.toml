[project]
name = "vapoursynth-plusplus-development"
version = "0.1.0"
description = "Pinned build and documentation tools for vapoursynth-plusplus."
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
build = [
    "meson==1.9.0",
    "ninja==1.13.0",
]
docs = [
    "zensical==0.0.52",
]
runtime-test = [
    "vapoursynth==78",
]
